-- ============ KEYBINDINGS ============

local wezterm = require("wezterm")
local act     = wezterm.action

local M = {}

M.config = {
	keys         = {},
	key_tables   = {},
	mouse_bindings = {},
}

return M
